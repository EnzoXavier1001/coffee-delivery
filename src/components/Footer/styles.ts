import styled from "styled-components";

export const FooterContainer = styled.footer`
    max-width: 112rem;
    margin: 0 auto;

    a {
        position: fixed;
        top: 92%;
        right: 14%;
    }
`